# W4 — Map legend + readability pass

**Branch:** `w4-map-legend` · ~40 min · pure demo legibility.
**Own:** NEW `src/components/map/Legend.tsx`, `src/components/MapView.tsx`.
Nothing else. (Palette values come from `./palette` — import, don't
duplicate.)

1. `Legend.tsx` — compact overlay card, absolute bottom-left of the map
   (the map container is `relative`): rows of swatch + label for Trail,
   Road, Creek, Steep, Probability (small red→amber gradient bar),
   Searched (gray), Claimed (colored outline), ◆ Tip, LKP (red dot).
   Bottom line, muted: "select a team, then click a cell to task it —
   press / to speak". Dark card (bg-black/70, backdrop-blur, border),
   font-mono 9–10px labels, uppercase tracking. It must read from the back
   of a room: don't shrink below 10px for the labels.
2. Mount it in `MapView.tsx` after the markers.
3. Readability tuning in `MapView.tsx` (small, surgical):
   - Terrain: raise trail/road/water `fill-opacity` toward 0.4 so the
     network reads as connected lines, not scattered squares; keep steep
     subtle.
   - Heatmap: lower `heatmap-radius` mid-zoom (~14 at z12) so mass hugs
     the trail shape instead of blobbing; nudge `heatmap-intensity` down
     if it whites out at the center.
   - Iterate against the live map — screenshot judgment beats numbers.
4. If (and only if) time remains: fade tip diamonds below weight 0.3 to
   30% opacity so stale intel visibly ages.

Accept: someone who has never seen Locus can look at the map for five
seconds and say what the colors mean and where the search is focused.
That sentence is the acceptance test.
