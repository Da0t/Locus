# W9 — Visual polish (app shell)

**Branch:** `w9-visual-polish` · ~40 min · make it look like a product,
not a hackathon. W4 owns the MAP's visuals (legend, terrain, heatmap
ramp) — you own everything AROUND the map. Do not touch MapView.tsx,
map/*, TipsFeed.tsx (W1), FoundOverlay.tsx (W5), StatusBar.tsx (W6),
App.tsx (W3).

**Own:** `src/index.css`, `src/components/ReasoningPanel.tsx`,
`src/components/voice/CommandConsole.tsx` (visual classes only — logic
stays), `src/components/Toaster.tsx`.

1. `index.css` theme pass: commit to the ops-room identity —
   near-black background with a subtle red-tinted vignette, one accent
   (amber) used ONLY for live/lead/alert, monospace numerals everywhere
   (`font-variant-numeric: tabular-nums`), thin borders at 10–15% white.
   Add two keyframes the other components already lean on: a soft
   `pulse-glow` and a `scanline` sweep you can apply to the sidebar
   header (subtle — if it's noticeable from 3m away it's too much).
2. `ReasoningPanel.tsx`: tighten to a briefing card — bigger weight
   numerals, thin weight bar with a soft glow on the leader, reasoning
   text in a quieter gray, timestamp of last re-weigh ("reasoned 12s
   ago") bottom-right in 9px mono.
3. `CommandConsole.tsx` (classes only): make the input feel like THE
   control surface — slightly taller, focused ring in accent color, the
   `( / to focus )` hint styled as a real keycap chip. Intent chips get
   consistent 9px mono uppercase treatment.
4. `Toaster.tsx`: claim-race toast becomes a bordered alert strip
   (top-center, accent border, mono) — it's a demo beat, style it like
   one.
5. Rule of taste: remove before adding. If a element has three fonts,
   two borders and a shadow, delete until it has one job. Screenshot at
   50% zoom — if the hierarchy survives, ship it.

Accept: side-by-side before/after screenshot posted to the team chat,
and no layout shifts — pure visual layer, zero logic diffs
(`git diff` should show className/CSS churn only).
