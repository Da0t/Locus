# W6 — Presence (coordinators online)

**Branch:** `w6-presence` · ~40 min · Convex presence checklist item.
**Own:** `convex/convex.config.ts` (add one line), NEW `convex/presence.ts`,
NEW `src/components/PresenceBar.tsx`, `src/components/StatusBar.tsx`
(mount only). Nothing else.

1. `npm i @convex-dev/presence` — then in `convex/convex.config.ts` add
   `app.use(presence)` (import from `@convex-dev/presence/convex.config`)
   next to the agent line. `npx convex dev --once` to regen.
2. `convex/presence.ts` — follow the component README
   (github.com/get-convex/presence): expose `heartbeat`, `list`,
   `disconnect` wrappers keyed by `caseId` as the room. Identity: no auth
   in this app — generate a stable per-tab name in the client
   (`coordinator-<adjective>` from sessionStorage) and pass it.
3. `src/components/PresenceBar.tsx` — `usePresence` hook per README:
   render up to 4 small initial-avatars + "N coordinators online",
   font-mono, muted; pulse-dot on your own avatar.
4. Mount at the left edge of `StatusBar.tsx` (it renders in the header).
   Touch nothing else there.

Accept: open two browser windows → both show "2 coordinators online"
within a heartbeat; close one → drops to 1 shortly after. Pairs on stage
with the two-tab claim-race beat: "same room, live presence, and the
database referees the conflict."

If the component fights you for more than 15 minutes, STOP and ship a
90%-cheaper fallback: a `presence` row per session heartbeat-patched via a
plain mutation + a query filtering `updatedAt > now-15s` (needs a table —
ping the integrator for the schema add before doing this).
