# W3 — Subject photo (Convex file storage)

**Branch:** `w3-subject-photo` · ~35 min · file-storage checklist item.
**Own:** NEW `convex/photos.ts`, NEW `src/components/SubjectPhoto.tsx`,
`src/App.tsx` (header only — mount the component next to the case name).
Nothing else.

1. `convex/photos.ts`:
   - `generateUploadUrl` mutation → `ctx.storage.generateUploadUrl()`.
   - `attach` mutation({caseId, storageId: v.id("_storage")}) → patch
     `cases.photoStorageId` (field already in schema).
   - `url` query({caseId}) → case's `photoStorageId` ?
     `ctx.storage.getUrl(id)` : null.
2. `src/components/SubjectPhoto.tsx`:
   - If `photos.url` returns a URL: 40px rounded portrait in the header,
     click → larger overlay (the searcher's "what does she look like").
   - Else: a small dashed "add photo" button → hidden file input → POST the
     file to `generateUploadUrl()` result → `attach`. Standard Convex
     upload pattern (see docs.convex.dev/file-storage/upload-files).
3. Mount in `src/App.tsx` header, left of the case name. Touch nothing
   else in the file.
4. Asset: grab any rights-free portrait-style placeholder (or generate a
   neutral silhouette PNG) into `public/subject-placeholder.png` and use it
   for the demo upload — do NOT use a real person's photo from the web.

Accept: upload once, photo appears in the header for EVERY connected tab
(storage + reactivity), survives reload, and the demo can say "her photo is
in every searcher's pocket."
